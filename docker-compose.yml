
services:
  app:
    build: .
    container_name: nextjs_app
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      - .:/app            
      - /app/node_modules
    environment:
      - WATCHPACK_POLLING=true
    command: npm run dev